let animals = ["cat", "lion", "panther"]
animals.forEach(animal => {
    if(animal === "cat"){
        console.log(`${animal} would make a great pet`);
    }else{
        console.log(`${animal}`);
    }
})